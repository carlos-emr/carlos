# `bc/` — British Columbia-only migrations

Generated by `database/mysql/build-baseline.sh`: `V1.0.1__bc_schema.sql` (from `oscarinit_bc.sql`)
and `V1.1.1__bc_seed.sql` (from `oscardata_bc.sql` + `bc_billingServiceCodes.sql` +
`bc_professionalSpecialists.sql` + `bc_pharmacies.sql`).

Applied together with `common/` for a BC install
(`flyway.locations=...common,...bc`). New BC-only schema changes go here as
`VYYYY.MM.DD.N__short_description.sql` with a `.N` suffix that orders after the shared change.
