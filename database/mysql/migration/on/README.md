# `on/` — Ontario-only migrations

Generated by `database/mysql/build-baseline.sh`: `V1.0.1__on_schema.sql` (from `oscarinit_on.sql`)
and `V1.1.1__on_seed.sql` (from `oscardata_on.sql` + `olis/olisinit.sql` + Ontario lookups).

Applied together with `common/` for an Ontario install
(`flyway.locations=...common,...on`). New Ontario-only schema changes go here as
`VYYYY.MM.DD.N__short_description.sql` with a `.N` suffix that orders after the shared change.
