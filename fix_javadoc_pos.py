import os
import re

# Okay, now the Javadoc IS before the annotations.
# Oh I see, earlier I ran a script that moved the Javadoc BEFORE the // FindSecBugs
# Before that script (which I didn't amend into the commit), the Javadoc was after the annotations.
# Let me double check if the commit c67048ce4 actually has Javadoc before annotations.
pass
