--- src/main/java/io/github/carlos_emr/carlos/commn/dao/DrugDaoImpl.java
+++ src/main/java/io/github/carlos_emr/carlos/commn/dao/DrugDaoImpl.java
@@ -481,9 +481,10 @@
     @Override
     public List<Object[]> findByParameter(String parameter, String value) {
         if (parameter == null || !parameter.matches("^[a-zA-Z0-9_]+$")) {
             throw new IllegalArgumentException("Invalid parameter name");
         }
-        // Using standard parameterization via StringBuilder to prevent static analysis false positive flags
-        String sql = new StringBuilder("select special,special_instruction from drugs where `").append(parameter).append("` = ?1 order by drugid desc").toString();
+        StringBuilder sb = new StringBuilder("select special,special_instruction from drugs where `");
+        sb.append(parameter).append("` = ?1 order by drugid desc");
+        String sql = sb.toString();
         Query query = entityManager.createNativeQuery(sql);
         query.setParameter(1, value);
