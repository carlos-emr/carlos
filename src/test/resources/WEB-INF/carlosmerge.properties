# Test-only fixture for StartupUnitTest issue #2969 regression (two-startup WEB-INF merge).
# Loaded via getResourceAsStream("/WEB-INF/carlosmerge.properties") when Startup falls back to the
# WEB-INF config. Uses a unique context name ("carlosmerge") so it cannot affect the existing
# shouldAbortStartup_whenNoConfigFileExists test, which drives context "carlos" and relies on the
# WEB-INF read failing. Synthetic values only — no real credentials or PHI.
db_username=carlos_test_user
db_name=carlos_test_db
